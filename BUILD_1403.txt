JianJian Ultimate — Build 1403
課表目標設定儲存版

新增：
- 「儲存目前設定」按鈕。
- 可保存目標、每週天數與偏好，而不重新建立課表。
- 儲存沿用原本 save() 流程：
  1. 先寫入 localStorage
  2. 已登入時再透過既有 syncCloud() 同步 Firestore
- 「生成並套用」也會同步保存三項設定。
- 重新開啟「我的」頁時會讀回已保存的設定。
- 顯示最後儲存時間。

操作：
- 只想記住選項：按「儲存目前設定」。
- 想重新生成課表與菜單：按「生成並套用」。
